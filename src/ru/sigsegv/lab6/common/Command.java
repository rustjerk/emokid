package ru.sigsegv.lab6.common;

public enum Command {
    ADD,
    ADD_IF_MAX,
    CLEAR,
    COUNT_GREATER_THAN_STUDIO,
    INFO,
    MIN_BY_STUDIO,
    PRINT_FIELD_ASCENDING_STUDIO,
    REMOVE_BY_ID,
    REMOVE_GREATER,
    REMOVE_LOWER,
    SHOW,
    UPDATE;
}
