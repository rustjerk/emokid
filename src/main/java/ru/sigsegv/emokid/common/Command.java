package ru.sigsegv.emokid.common;

public enum Command {
    ADD,
    ADD_IF_MAX,
    CLEAR,
    COUNT_GREATER_THAN_STUDIO,
    INFO,
    LOGIN,
    MIN_BY_STUDIO,
    PRINT_FIELD_ASCENDING_STUDIO,
    REGISTER,
    REMOVE_BY_ID,
    REMOVE_GREATER,
    REMOVE_LOWER,
    SHOW,
    SUBSCRIBE,
    UPDATE
}
